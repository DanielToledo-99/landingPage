# Supabase — create a free project at https://supabase.com
# Copy your Project URL and anon/public key from: Project Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Google reCAPTCHA v2 — register at https://www.google.com/recaptcha/admin
# The default key below is a Google test key (always passes in development)
VITE_RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI

# Supabase table setup — run this SQL in the Supabase SQL Editor:
# CREATE TABLE leads (
#   id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
#   name TEXT NOT NULL,
#   email TEXT NOT NULL,
#   phone TEXT NOT NULL,
#   address TEXT NOT NULL,
#   language TEXT DEFAULT 'en'
# );
# ALTER TABLE leads ENABLE ROW LEVEL SECURITY;
# CREATE POLICY "Allow inserts from anyone" ON leads FOR INSERT WITH CHECK (true);
